FROM python:3.10-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY . .

RUN mkdir -p /app/output /app/logs

ENV PYTHONPATH=/app/src
ENV PORT=8080

CMD ["python", "src/scheduler_server.py"]
